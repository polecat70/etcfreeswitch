
<include>

	<gateway name="eutelia">

		<!-- // account username *required* // -->
		<param name="username" value="0683394902"/>
	
		<!-- // auth realm: *optional* same as gateway name, if blank // -->
		<param name="realm" value="voip.eutelia.it"/>
		
		<!-- // username to use in from: *optional* same as  username, if blank // -->
		<param name="from-user" value="0683394902"/>
		
		<!-- // domain to use in from: *optional* same as  realm, if blank // -->
		<param name="from-domain" value="voip.eutelia.it"/>

		<!-- // account password *required* // -->
		<param name="password" value="31vYh7jY"/>
		
		<!-- // extension for inbound calls: *optional* same as username, if blank // -->
		<param name="extension" value="0683394902"/>
	
		<!-- // expire in seconds: *optional* 3600, if blank // -->
		<param name="expire-seconds" value="120"/>
	
		<!-- CHANGED THIS-->
		<!-- // do not register // -->
		<param name="register" value="true"/>

		<!-- CHANGED THIS-->
		<!-- // which transport to use for register -->
		<param name="register-transport" value="udp"/>

		<!-- // How many seconds before a retry when a failure or timeout occurs -->
		<param name="retry-seconds" value="30"/>

		<!-- // Use the callerid of an inbound call in the from field on outbound calls via this gateway  -->
		<param name="caller-id-in-from" value="false"/>

		<!-- // extra sip params to send in the contact -->
		<param name="contact-params" value=""/>

		<!-- // Put the extension in the contact -->
		<param name="extension-in-contact" value="true"/>

		<!-- // send an options ping every x seconds, failure will unregister and/or mark it down -->
		<param name="ping" value="25"/>
		<param name="cid-type" value="rpid"/>
		
		<!-- // rfc5626 : Abilitazione rfc5626 // -->
		<param name="rfc-5626" value="true"/>
		
		<!-- // rfc5626 : extra sip params to send in the contact -->
		<param name="reg-id" value="1"/>
	
	</gateway>
</include>
